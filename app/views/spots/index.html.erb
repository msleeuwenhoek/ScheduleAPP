
<table class="table">
  <thead>
    <tr>
      <th scope="col">Spot ID</th>
      <th scope="col">Period</th>
      <th scope="col">Weekday</th>
      <th scope="col">Time</th>
      <th scope="col">Index</th>
      <th scope="col">Student</th>
      <th scope='col'>Lesson count</th>
      
    </tr>
  </thead>
  <tbody>
    <% @spots.each do | spot | %>
    <tr>
      <th scope="row"><%= spot.id %></th>
      <td><%= spot.period %></td>
      <td><%= spot.weekday %></td>
      <td><%= spot.time %></td>
       <td><%= spot.spot_index_number %></td>
      <td><%= spot.user.full_name %></td>
      <td><%= spot.lesson_amount %></td>
     <td><%= link_to 'Show', spot_path(spot)%> <%= link_to 'Edit', edit_spot_path(spot)%></td>
</div>
    </tr>
    <% end %>
  </tbody>
</table>
